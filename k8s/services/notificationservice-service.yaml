apiVersion: v1
kind: Service
metadata:
  name: notificationservice
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 2342
      targetPort: 2342
  type: ClusterIP
