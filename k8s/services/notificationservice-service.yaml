apiVersion: v1
kind: Service
metadata:
  name: notificationservice
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 2342
  type: ClusterIP
